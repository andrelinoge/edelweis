.notifications.top-right

- unless notice.blank?
  javascript:
    $(document).ready(function () {
      $('.top-right').notify({
        message: { text: "#{notice}" }
      }).show();
    });